

$(function() {
  $('.advertisement-box').slick({
      dots: true,
      // 下のドット表示
      

      autoplay: true,
      // 自動再生。trueで自動再生される。
      speed: 400,
      // 自動再生や左右の矢印でスライドするスピード
      autoplaySpeed: 3000,
      // 自動再生で切り替えをする時間
      accessibility: true,
      // アクセシビリティ。左右ボタンで画像の切り替えをできるかどうか
  });
});










